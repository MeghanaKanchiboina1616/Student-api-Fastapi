version: "3"

services:

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: studentsdb

  app:
    build: .
    ports:
      - "8000:8000"

    depends_on:
      - db